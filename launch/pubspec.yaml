name: launch
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  demo:
    path: ../demo
  app:
    path: ../app
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  tg_lint_rules:
    path: ../tg_lint_rules

  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true