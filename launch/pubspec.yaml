name: launch
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  app:
    path: ../app
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  showcase:
    path: ../showcase

dev_dependencies:
  tg_lint_rules:
    path: ../tg_lint_rules

flutter:
  uses-material-design: true