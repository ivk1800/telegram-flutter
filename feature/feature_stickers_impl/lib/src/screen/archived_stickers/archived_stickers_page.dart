import 'package:flutter/material.dart';

class ArchivedStickersPage extends StatelessWidget {
  const ArchivedStickersPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text('ArchivedStickers'),
      ),
    );
  }
}
