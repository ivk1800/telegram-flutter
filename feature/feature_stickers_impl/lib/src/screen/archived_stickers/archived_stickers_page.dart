import 'package:flutter/material.dart';

class ArchivedStickersPage extends StatelessWidget {
  const ArchivedStickersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('ArchivedStickers'),
      ),
    );
  }
}
