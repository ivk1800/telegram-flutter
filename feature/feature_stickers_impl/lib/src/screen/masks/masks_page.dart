import 'package:flutter/material.dart';

class MasksPage extends StatelessWidget {
  const MasksPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('Masks'),
      ),
    );
  }
}
