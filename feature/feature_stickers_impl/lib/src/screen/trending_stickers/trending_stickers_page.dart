import 'package:flutter/material.dart';

class TrendingStickersPage extends StatelessWidget {
  const TrendingStickersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('TrendingStickers'),
      ),
    );
  }
}
