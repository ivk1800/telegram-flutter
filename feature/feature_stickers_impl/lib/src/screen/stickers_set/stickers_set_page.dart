import 'package:flutter/material.dart';

class StickerSetPage extends StatelessWidget {
  const StickerSetPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text('StickerSetPage'),
      ),
    );
  }
}
