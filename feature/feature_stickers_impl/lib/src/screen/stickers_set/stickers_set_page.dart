import 'package:flutter/material.dart';

class StickerSetPage extends StatelessWidget {
  const StickerSetPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('StickerSetPage'),
      ),
    );
  }
}
