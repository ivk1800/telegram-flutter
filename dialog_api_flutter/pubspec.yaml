name: dialog_api_flutter
publish_to: 'none'

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ^3.0.0

dependencies:
  dialog_api:
    path: ../dialog_api
  flutter:
    sdk: flutter

dev_dependencies:
  tg_lint_rules:
    path: ../tg_lint_rules